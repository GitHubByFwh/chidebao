server:
  port: 8080
spring:
  application:
#    应用的名称，可选
    name: supermarkets
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/supermarket?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: root
      password: mysql
mybatis-plus:
  configuration:
    #在映射实体或者属性时，将数据库中表名和字段名中的下划线去掉，按照驼峰命名法映射
    map-underscore-to-camel-case: true
#    显示SQL语句
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: ASSIGN_ID
#      逻辑删除配置
#      logic-delete-field: isDeleted
#      logic-delete-value: 1 #逻辑已删除
#      logic-not-delete-value: 0 #逻辑已删除

chiDeBao:
  path: E:\BaiduNetdiskDownload\瑞吉外卖\1 瑞吉外卖项目\资料\img\